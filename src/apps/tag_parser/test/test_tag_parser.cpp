#include <catch.hpp>

#include "../tag_parser.h"

TEST_CASE("set_duplicates1", "[util]")
	{
	
	REQUIRE(1 == 1);
	REQUIRE(true);
	}


TEST_CASE("set_duplicates_1", "[util_1]")
	{
	tag_parser();
	REQUIRE(true);
	}